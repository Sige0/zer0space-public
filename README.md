# zer0space-public
zer0space is my homelab setup: Docker Swarm across 7 Debian nodes, GitOps-managed configs via GitHub, deployed with Claude Code. Public access via Cloudflare Tunnel + reverse proxy (origin IP hidden), remote management via WireGuard/Tailscale + web dashboard.
